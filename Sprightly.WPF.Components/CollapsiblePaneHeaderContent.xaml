<UserControl x:Class="Sprightly.WPF.Components.CollapsiblePaneHeaderContent"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid HorizontalAlignment="Stretch">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <TextBlock Text="{Binding Path=PanelIcon, Mode=OneWay}" 
                   FontFamily="fa-regular-400.ttf#Font Awesome 5 Free Regular"
                   Grid.Column="0" 
                   HorizontalAlignment="Stretch" 
                   TextAlignment="Left"
                   Margin="8.0, 4.0, 8.0, 4.0"/>
        <TextBlock Text="{Binding Path=HeaderText, Mode=OneWay}" 
                   Grid.Column="1" 
                   HorizontalAlignment="Stretch" 
                   TextAlignment="Left"
                   Margin="8.0, 4.0, 8.0, 4.0"/>
    </Grid>
</UserControl>
